# Test task for MobiChord

### Install
`sudo npm install -g mobichord-test`

### Usage
`mobichord-test <input.zip> <output.json>`

 There is default input .zip file in the package. U can use it typing:

 `mobichord-test zip.zip <output.json>`